{
  "name": "joseki-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "compile": "astro build",
    "preview": "astro preview",
    "verify": "npm run compile && npm --prefix test run verify",
    "deploy": "npm run compile && npx --yes wrangler@4 pages deploy dist --project-name thejoseki --branch main --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.3"
  }
}
